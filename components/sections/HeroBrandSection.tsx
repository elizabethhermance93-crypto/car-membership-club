"use client";

import { Hero } from "@/components/sections/Hero";

/** Wrapper for hero; Hero owns 6-banner state and logo selector internally. */
export function HeroBrandSection() {
  return <Hero />;
}
